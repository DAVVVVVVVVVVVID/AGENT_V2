# Chat 模块设计方案

> 本文档记录 AGENT_v2 多人对话系统的完整设计，包含设计讨论过程、得出的结论及其依据。

---

## 一、设计目标

在沙盒世界中实现 player 之间的自然对话机制，覆盖三种场景：
- **Agent ↔ Agent**：两个或多个 agent 之间的交流
- **Human ↔ Agent**：人类玩家与 agent 之间的交流
- **Human ↔ Human**：人类玩家之间通过沙盒进行的交流

核心原则：**无论是人类还是 agent，其操控的 player 在沙盒中能进行的对话行为和行为方式完全统一**。沙盒只感知"player 的行为"，不区分背后是人类还是 agent。

---

## 二、关键设计决策

### 2.1 消息在世界层流通（而非元层）

**结论**：对话作为沙盒内的世界事件发生，而非通过外部系统注入。

**理由**：消息在世界层流通，agent 通过 Perceive 感知到"有人向我发起对话"，这和感知到"正前方有一个床"是同等层次的世界事件。这样：
- Agent 不需要意识到自己在被外部操控，行为语义一致
- 人类和 agent 使用完全相同的底层机制（对话 API），行为对等
- 对话记录自然地成为世界历史的一部分，可以形成记忆

### 2.2 消息进入 Perceive，但判断独立

**结论**：打招呼/对话请求事件进入 Perceive，但不直接混入普通感知内容作为 prompt 的一部分；而是触发一个独立的判断模块。

**理由**：如果把"有人向你打招呼：早上好"和"正前方是床"混在同一个 prompt 里，agent 会用 Think 工具（移动、交互等）来"回应"，语义错乱。打招呼事件具有更高的优先级语义，应该触发专门的判断流程。

### 2.3 对话使用独立模块，不复用 Think

**结论**：对话期间 agent 进入专用对话模块，有独立的 prompt 设计、独立的工具集、独立的循环结构。

**理由**：Think 模块的工具集（移动、交互物品、finish 等）和对话场景完全不同。复用 Think 会导致工具语义混乱，也无法针对对话场景优化 prompt（如如何结合记忆、如何体现性格）。

### 2.4 Chat 记忆独立类型，Recognition 正常生成

**结论**：对话结束后生成两类记忆——新的 `chat` 类型记忆记录"发生了什么"，`recognition` 类型记忆记录"我对这个人的主观态度和判断"。

**理由**：event 记忆的格式（What/How/Why/Raw Actions）是为行动序列设计的，不适合记录对话内容。对话记忆的核心是"谁说了什么、达成了什么共识、我得到了什么信息"，结构差异足够大，值得单开一个类型。Recognition 记忆则用来记录对话方给自己留下的印象，这和普通的 recognition（对世界规律的认知）在格式上是兼容的。

### 2.5 Chat 记忆计入 Dream 触发计数

**结论**：chat 类型记忆参与 Dream 触发的记忆数量统计（与 event/recognition 相同）。

**理由**：一次有实质内容的对话可能比几次普通 Plan 更值得整合。排除 chat 记忆会导致 Dream 低估记忆积累量，触发频率偏低。

### 2.6 find_nearby_players 与 send_chat_request 拆开

**结论**：提供两个独立工具，而非合并为一个。

**理由**：find 是查询类操作，send 是动作类操作，语义不同。拆开后 agent 可以先查到范围内有哪些 player，根据结果决定是否发起请求、向谁发起——不是查到人就必须发。合并成一个工具会剥夺这个判断空间。

---

## 三、沙盒层对话机制

### 3.1 对话范围

Player 可以向一定范围内的其他 player 发起对话请求。范围建议以**同一个 Arena（场所）**为单位——比"3×3 tiles"更自然，不需要精确计算距离，也符合"在同一个房间里才能对话"的现实直觉。

具体范围的精确定义（tiles 半径 vs Arena vs Sector）留待实现时确定，但建议不要太大（不宜跨 Sector）。

### 3.2 发起对话请求

发起者选择范围内的一个或多个 player，附带一句**开场白**，发送对话请求。

**重要约束**：开场白不应包含关键信息或强信息内容（如"我需要你帮我做X"）。开场白的作用是"敲门"而非"传递信息"，因为接收方可能拒绝，重要信息不应在请求阶段暴露。这个约束需要在 agent 的对话工具描述中明确说明。

示例：
- 合适："早上好"、"我想跟你聊一聊"、"有空吗"
- 不合适："我想问你冰箱怎么用"（包含了具体信息诉求）

### 3.3 接受与拒绝请求

收到请求的 player 看到发起者身份和开场白，可以：

**接受**：提供一句回应话语（成为对话的第二句话），双方进入聊天室。
- 示例：请求"早上好" → 接受"早上好啊，最近在做什么？"

**拒绝**：提供一句拒绝理由，请求失败，双方继续各自活动。
- 示例：请求"我可以和你聊聊吗" → 拒绝"抱歉，我正在忙"

### 3.4 对话期间的 Buff

进入对话后，所有参与 player 自动获得以下 buff（沙盒层施加）：
- 无法移动
- 无法使用/退出物品
- 无法发起新的交互

**设计理由**：避免"边聊天边干活"的状态，保持行为一致性——人类和 agent 在对话期间都被锁定，不存在 agent 一边对话一边执行 Plan 的情况。

### 3.5 聊天室

双方进入共享聊天室后：
- 不强制一人一句，双方自由发言
- 任意一方可以随时选择退出
- 退出时聊天室显示提示："[player名] 退出了对话"
- 当所有参与者都退出后，聊天室关闭，对话正式结束

### 3.6 多人对话

发起者可以同时向范围内多个 player 发出邀请，所有收到邀请的 player 独立决定是否加入。所有 player 完成选择后，接受者们进入同一个聊天室。退出逻辑与双人相同。

**发言顺序问题**：多人聊天室中多个 agent 同时运行各自的对话循环，可能出现"同时发言"的情况。解决方式：每个 agent 在决定发言前引入一个随机短暂延迟（如 0.5–2 秒），在这个窗口期内如果其他人已经发言，将新消息纳入本轮感知再做决定。这不是严格轮次制，但能有效减少撞发概率，且实现简单。

---

## 四、人类玩家视角

### 4.1 发起对话

1. 控制角色移动至目标 player 所在的对话范围内
2. 界面出现"发起对话"按钮
3. 点击后弹出 player 列表（当前范围内所有可对话 player，支持复选）
4. 输入开场白，点击发送

### 4.2 接收请求

弹窗提示，显示：
- 发起者名称
- 开场白内容
- 文本输入框（填写回应或拒绝理由）
- "接受"和"拒绝"两个按钮

### 4.3 对话界面

类似微信/QQ 的聊天室布局：
- 消息列表（显示发言者名称 + 内容 + 时间）
- 文本输入框 + 发送按钮
- 退出对话按钮

---

## 五、Agent 视角

### 5.1 Agent 主动发起对话

**触发时机**：有两种情况都应支持：
1. **Plan 驱动**：Planner 生成了社交类 Plan（如"去找小刚聊一聊"），Think 执行该 Plan 时调用对话工具
2. **Think 自发判断**：在执行某个 Plan 的过程中，agent 判断"我需要问问小刚"，自发调用对话工具

**所需工具（两个，拆开）**：
- `find_nearby_players()` → 返回当前范围内可对话的 player 列表（id、名称、状态）
- `send_chat_request(player_ids: list, greeting: str)` → 向指定 player 发起对话请求，附带开场白

工具描述中需明确说明：开场白不应包含关键信息，仅作敲门用途。

### 5.2 Agent 感知对话请求

打招呼/对话请求事件在 Perceive 阶段被感知到，但**不混入普通感知内容**，而是作为独立字段传入。

当 Perceive 结果包含待处理的对话请求时，进入独立的**判断模块**，暂停主循环的下一步（不执行 Retrieve → Think）。

### 5.3 判断模块

一次轻量的独立 LLM 调用，判断是否接受对话请求。

**输入**：
- 当前 Plan 内容及已执行轮次（进度信息）
- 请求发起者的身份（entity_id、名称）
- 与发起者相关的历史记忆（检索，主要是 chat 记忆和 recognition 记忆）
- 请求的开场白内容

**输出**（三选一）：
- 接受（附一句回应话语）
- 拒绝（附一句拒绝理由）
- 暂缓（可选，如"等我完成当前动作"，实现复杂度较高，初期可以不支持）

判断应综合考虑：当前 Plan 的重要性和进度、发起者是否熟悉、对话时机是否合适。

### 5.4 对话循环

接受对话后，agent 进入独立的对话循环，完全脱离主 Plan 循环。

```
while 对话进行中:
    perceive_chat()       # 获取聊天室中的新消息、退出事件
    retrieve_memories()   # 检索与发言者、当前话题相关的记忆
    decide()              # speak / wait / exit
```

**工具集（两个）**：
- `speak(content: str)` → 在聊天室发言
- `exit_chat(reason: str = "")` → 退出当前对话

**对话 prompt 设计要点**：
- 使用 agent 的性格特征（OCEAN）影响说话风格
- 注入检索到的相关记忆（尤其是与对方的历史记忆、recognition）
- 包含完整的当前聊天记录作为上下文
- 明确说明 agent 可以选择不立即回复（wait）

### 5.5 等待机制

Agent 不必对方说一句就立即回应，可以选择 `wait`——等待对方把话说完，或观察群聊动态。

**实现方式**：
- 每次 perceive_chat 批量收取自上次发言以来的所有新消息
- Agent 的输出：`speak` / `wait` / `exit` 三选一
- 连续 `wait` 超过 N 次（建议 N=3）后，系统向 agent 注入一条中性提示：

  > "你已经有一段时间没有发言了。"

  措辞刻意保持中性，不暗示"要不要退出"，让 agent 自由决定接下来说话还是退出。超过上限后强制进入下一轮循环，不允许继续 wait。

### 5.6 感知退出事件

其他 player 退出聊天室时，该事件出现在 perceive_chat 的结果中（如 `{"type": "player_exit", "player": "小刚"}`），agent 在下一轮循环中感知到，自行决定是否继续对话或跟着退出。

---

## 六、记忆系统

### 6.1 Chat 记忆（新类型）

对话结束时，异步触发类似 Shadow Agent 的"对话整理模块"，生成一条 `chat` 类型记忆。不存储原始对话记录（太长，大量内容对检索无价值），而是提炼结构化摘要。

**文件格式**：

```
---
Type: chat
Participants: [entity_id_1, entity_id_2, ...]
Time: {ISO 时间}
Summary: {一句话概括}
Importance: {整数}
Keywords: [{关键词列表}]
---

## 主题
聊了什么，为什么开始聊

## 关键信息
对方透露的重要信息，或对我有参考价值的内容

## 结果
对话如何结束，是否影响当前计划或目标
```

**存储位置**：`memory/chats/chat_NNN.md`（新增 `chats/` 目录）

**索引**：与 event/recognition 相同，写入 Memory.md 和 keywords.md

### 6.2 Recognition 记忆

对话结束后，agent 正常生成 recognition 类型记忆，记录对对话方的主观态度和判断：
- 对这个人的印象（友善/冷漠/值得信任/…）
- 从对话中得到的启发或认知
- 下次和这个人互动时的注意事项

这与普通 recognition 格式兼容，不需要新类型。

### 6.3 Chat 记忆计入 Dream 触发

`chat` 类型记忆参与 Dream 触发的记忆数量统计，与 event/recognition 同等对待。

**理由**：一次有实质内容的对话积累的认知量可能超过几次普通行动，排除 chat 记忆会导致 Dream 低估记忆积累量，触发频率偏低。

---

## 七、对话结束后的处理流程

对话结束（所有参与者退出聊天室）后，按以下顺序处理：

```
1. 异步触发对话整理模块
   → 生成 chat 记忆
   → agent 自行生成相关 recognition 记忆（在下一步前完成）

2. Dream 检查
   → 调用 should_dream()
   → 如果触发，先执行 Dream，再继续
   → Dream 期间可整合对话中新生成的 chat/recognition 记忆

3. 重新定向判断（轻量 LLM 调用）
   → 输入：原 Plan 内容 + 当前状态 + 对话摘要
   → 输出：继续原 Plan / 放弃当前 Plan 重新规划
   → 理由：对话可能改变了 agent 的认知或目标
     （如对方告知"冰箱是坏的"，继续"验证冰箱交互"的 Plan 就没有意义了）

4. 回到主循环
```

### Dream 期间的处理

对话期间 Dream 自然不会触发——agent 不在主循环里，没有 Plan 完成，`should_dream()` 的检查时机不会到来，无需额外的抑制逻辑。对话结束后的 Dream 检查是自然补漏机制。

---

## 八、架构总览

```
【Agent 主动发起对话】
Think 阶段
  → find_nearby_players() 查询范围内 player
  → 决定是否发起请求
  → send_chat_request(player_ids, greeting)
  → 等待响应（阻塞或轮询）
  → 若被接受，进入对话循环（见下）

【Agent 被动接收请求】
Perceive
  └─ 发现待处理对话请求
       → 判断模块（独立 LLM 调用）
           输入：当前 Plan 进度 + 发起者记忆 + 开场白
           输出：接受 / 拒绝 / 暂缓
       → 拒绝：返回主循环
       → 接受：进入对话循环

【对话循环】
while 对话进行中:
    perceive_chat()
      → 批量收取新消息 + 退出事件
    retrieve_memories()
      → 检索与对方、话题相关的记忆
    decide()
      → speak(content)  发言
      → wait            本轮不发言（最多连续 N 次）
      → exit_chat()     退出对话

【对话结束后】
  1. 异步生成 chat 记忆 + recognition 记忆
  2. Dream 检查（如触发先 Dream）
  3. 重新定向判断（续 Plan / 重新规划）
  4. 回到主 Plan 循环
```

---

## 九、待定与待实现细节

以下内容讨论中未完全确定，实现时需进一步决策：

| 问题 | 当前倾向 | 待确认 |
|------|---------|--------|
| 对话范围 | 同一 Arena | 是否需要支持更大范围（同 Sector）？ |
| 等待上限 N | 建议 3 次 | 需测试调整 |
| "暂缓"接受 | 初期不支持 | 实现复杂，后续考虑 |
| 对话整理模块 | 类 Shadow Agent 异步触发 | prompt 和工具定义待设计 |
| 重新定向判断 | 轻量 LLM 调用 | 具体 prompt 待设计 |
| 沙盒 API 扩展 | 需新增对话相关接口 | 接口设计待定，需更新 api-spec.md |
| chat 记忆的 importance 计算 | 参考对话内容质量 | 具体计算方式待定 |
